defmodule Kubeojo.JenkinsView do
  use Kubeojo.Web, :view
  #def job_name({jobname: jobname}) do
  #          jobname
            #            |> String.split(" ")
            #|> Enum.at(0)

            #end
end
